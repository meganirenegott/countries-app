==================================
🧠 VERSION 2 PSEUDO-CODE WORKSHEET
==================================

This is our WHAT, WHEN, WHERE, HOW framework for working with data:

1. WHAT data should we store & retrieve?
   What kind of HTTP request do we need to send, and to what API endpoint?
   If we are sending a POST request, what do we need to send in the request body, and what is the data type?
   What would we get back in the response, and what is the data t ype?

    - GET request, (no request body) has base URL, with endpoint /add-one-user
    - POST requests contain the request body
    - (if the user is already logged in, form should disappear with message "welcome back (userName with latest user) refresh with a quick load screen, there is a little bit of a lag because there is a sleepy API, 
    and we have a conditional render, like only render the form when there is a newest user)

2. WHEN should we store & retrieve the data?
   What event or user action is the trigger, such as clicking a button, submitting a form, or loading a page?

3. WHERE does this happen in the code?
   What component, page or function?

   - where the form lives, so the SavedCountries page

4. HOW do we accomplish this?
   What steps do we need to take?
   Do we need to...
   - make any GET or POST requests to an API endpoint?
   - render anything in JSX?
   - make any functions? If so, when would we call those functions?
   - use any useState or useEffect hooks?
     Remember: This is pseudo-code. Don't write code yet — just outline the actions in plain language.

✨ YOUR TASK: Fill in the answers to each WHAT/WHEN/WHERE/HOW question below for the 3 pieces of data.

==================================
📊 FORM DATA
==================================

========================
🔶 STORING FORM DATA
========================

❓ WHAT...
TYPE OF HTTP REQUEST (GET OR POST): ------ POST
API ENDPOINT: ------ /add-one-user

REQUEST BODY DATA TYPE (if applicable): ---- Object
REQUEST BODY (if applicable): ------ 
  {
    "name": "John Smith",
    "country_name": "Canada",
    "email": "john@example.com",
    "bio": "Another bio"
  }


RESPONSE DATA TYPE: ------ String
RESPONSE: ------ Success! User has been added.


❓ WHEN: ------ when the user submits the form

❓ WHERE: ------ the function we write will run from the handleSubmit() function

❓ HOW:
Step 1: ------ write a function to store Form Data call it userFormData, async function, using await 
Step 2: ------ use a post request
Step 3: ------ call the formUserData from handleSubmit()
Add more steps as needed!

========================
🔶 RETRIEVING FORM DATA
========================

❓ WHAT...
TYPE OF HTTP REQUEST (GET OR POST): ------ GET
API ENDPOINT: ------ /get-newest-user

REQUEST BODY DATA TYPE (if applicable): n/a
REQUEST BODY (if applicable): ------ n/a

RESPONSE DATA TYPE: ------ array with one object
RESPONSE: ------

[
  {
    "user_id": 42,
    "name": "John Smith",
    "country_name": "Canada",
    "email": "john@example.com",
    "bio": "Another bio"
  }
]


❓ WHEN: ------ when the SavedCountries page loads

❓ WHERE: ------ SavedCountries page

❓ HOW:
Step 1: ------ someone needs to fill out the form and submit it so we have some newest user data
Step 2: ------ make a function called getNewestUserData, it's going to be an async function, this function will use fetch to send a GET request to the endpoint /get-newest-user 
once it gets that data, then the function saves the data to a state variable. we can call it newestUserData
Then we can call the function on page load with useEffect
 API call with useEffect
Step 3: ------  we will use useEffect to call getNewestData on page load
Step 4: ------- render the newest users name in JSX
Add more steps as needed!

===================================
📊 SAVED COUNTRIES DATA
===================================

========================
🔶 STORING SAVED COUNTRIES DATA
========================

❓ WHAT...
TYPE OF HTTP REQUEST (GET OR POST): ------  Post
API ENDPOINT: ------ /save-one-country

REQUEST BODY DATA TYPE (if applicable): ------ object
REQUEST BODY (if applicable): ------ {
  "country_name": "Brazil"
}

RESPONSE DATA TYPE: ------ string
RESPONSE: ------ Success! The country is saved.

❓ WHEN: ------ the user wants to add a country

❓ WHERE: ------ SavedCountries.jsx

❓ HOW:
Step: 0: Make the save countries button/ UI
Step 1: ------ write a function to store Saved Countries Data, async function, using await
Step 2: ------ use a post request with the post method, do a stringify situation, event handler is going to be when the user clicks the "save countries" button
Step 3: ------ call the saveOneCountry() function from onClick
Add more steps as needed!
Step 4: ---- test your countries, check console's network tab to see if the API fetch is working as expected
Step 5: ----- expected behavior looks like the user selected country shows up in the saved countries page
(you can use Postman at any step, it's helpful even before you write code to see what the data you're working with looks like, to help visualize the data as it comes in)


========================
🔶 RETRIEVING SAVED COUNTRIES DATA
========================

❓ WHAT...
TYPE OF HTTP REQUEST (GET OR POST): ------  Get
API ENDPOINT: ------ /get-all-saved-countries

REQUEST BODY DATA TYPE (if applicable): ------ none
REQUEST BODY (if applicable): ------ none

RESPONSE DATA TYPE: ------ array of objects
RESPONSE: ------ [
  {
    "country_name": "Japan"
  },
  {
    "country_name": "Germany"
  }
]

❓ WHEN: ------ on load for Saved Countries Page

❓ WHERE: ------ Saved Countries page

❓ HOW:
Step 1: ------  someone will click on the save countries button
Step 2: ------  make a function called displayUserSavedCountries(), it's going to be an async function, this function will use fetch to send a GET request to the endpoint /get-all-saved-countries
once it gets that data, the function saves the data to a state variable which we will call userSavedCountries
Step 3: ------  we will use useEffect to call displayUserSavedCountries() onClick
Step 4: ------ render the userSavedCountries in JSX
Add more steps as needed!

=================================
📊 COUNTRY COUNTS DATA
=================================

========================
🔶 STORING & RETRIEVING COUNTRY COUNTS DATA
========================
NOTE: We combine storing & retrieving country counts data into one section because they always happen together. Each time we look up a country’s view count, we also need to increase it and save the new total.

❓ WHAT...
TYPE OF HTTP REQUEST (GET OR POST): ------  Post
API ENDPOINT: ------ /update-one-country-count

REQUEST BODY DATA TYPE (if applicable): ------  array of objects
REQUEST BODY (if applicable): ------  {
  "country_name": "France"
}

RESPONSE DATA TYPE: ------ string
RESPONSE: ------ {
  "count": 3
}
}

❓ WHEN: ------  when the user clicks the save country button

❓ WHERE: ------ on the country details page

❓ HOW:

Step 0: ------ write a function updateUserCountryCount() to post the the endpoint /update-one-country-count using async and await, useEffect is updated when the user clicks on the 
country card to view it
Step 1: ------ write a function called displayUserCountryViewCount() get the updated country count and rerender the count to update the UI
Step 2: ------ test your userCountryViewCount()
Step 3: ------  expected behavior looks like the count increasing by +1 on view
Step 4: ------  be sure remove strict mode fron the main.jsx file that vite adds on setup, so country count is incrementing in values of 1 instead of 2 

